{#let r=resource}
{#if r.multilineEffect}
```ad-inline-attack
title: {r.rangeType} {r.activity} {r.name.capitalized}{r.bonus.prefixSpace}{#if r.traits} ({r.traits}){/if}
{#if r.damage}
**Damage** {r.damage}  
{/if}{#if r.multilineEffect}
**Effect** {r.multilineEffect}  
{/if}
```
{#else}
- **{r.rangeType}** {r.activity} {r.name}{r.bonus.prefixSpace}{#if r.traits} ({r.traits}){/if}{#if r.damage}, **Damage** {r.damage}{/if}
{/if}
